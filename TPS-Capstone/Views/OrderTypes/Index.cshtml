@model IEnumerable<TPS_Capstone.Models.OrderType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
